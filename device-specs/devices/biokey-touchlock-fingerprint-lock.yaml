# BIO-key TouchLock BT family — Device Spec (BLE fingerprint/keyless locks)
# Reverse-engineered from the Android app com.champion.lock v2.2.1
# (versionCode 114, updated 2023-11-09), decompiled with jadx.
#
# Verification APK (obtained via the idongdong mirror's CDN, 2026-08):
#   sha256 ab6084e6b5c66c7d39ca670837427e8833c16e4cc01dfdba11737f2016990a77
#   (3,795,119 bytes)
#
# Key source classes (facts re-checked against the decompile, 2026-08):
#   com/champion/lock/base/BaseScanActivity.java       (GATT UUIDs)
#   com/champion/lock/base/BluetoothLeService.java     (write type, bond/unbond)
#   com/champion/lock/base/BaseBluetoothActivity.java  (session flow)
#   com/champion/lock/utils/CommandUtils.java          (20-byte frames, opcodes, checksum)
#   com/champion/lock/utils/DeviceNameUtils.java       (advertised-name patterns)
#   com/champion/lock/utils/Utils.java                 (time-derived password, status byte)
#   com/champion/lock/enums/ErrorCode.java             (response error codes)
#
# Nothing here has been replayed against a lock; no over-the-air capture
# exists. Every byte-level fact is read out of the decompiled app, and the
# lock-side details flagged in `remaining_unknowns` need hardware.

device:
  name: "BIO-key TouchLock BT family"
  manufacturer: "BIO-key International, Inc. (OEM app package com.champion.lock; Play Store developer Keyssmart)"
  manufacturer_status: "abandoned"
  testing:
    status: untested
    notes: >
      No hardware unit and no over-the-air capture. All protocol facts come
      from static analysis of the decompiled official Android app
      com.champion.lock v2.2.1 (sha256 in `sources`); UUIDs, opcodes, frame
      layout, checksum and name patterns were re-checked against the jadx
      output on 2026-08. Lock-side behaviour (status-byte polarity, bond
      enforcement, advert layout) is unverified.
  category: "lock"
  type: "ble_padlock_family"
  protocol: "ble"
  transport: "ble-gatt"
  models:
    - "BL0509 TouchLock BT XS (padlock)"
    - "BL1209 TouchLock BT XL (padlock)"
    - "BS1609 TouchLock TSA BT (TSA luggage lock)"
    - "TouchLock bike lock (advertises Bike_BT / Bike_Pr)"
    - "TouchLock U-lock (advertises U_Lock / ULockPr)"
    - "OEM rebadges speaking the same protocol (e.g. locks advertising Joi_Lock)"
  notes: >
    Family of Bluetooth Low Energy fingerprint/keyless locks sold under the
    BIO-key TouchLock BT brand (launched CES 2017 per BIO-key SEC filings).
    The TouchLock consumer hardware line appears discontinued — the product
    pages are gone from bio-key.com — but BIO-key International itself is
    still in business. FCC ID 2AIKJ-BL (grantee BIO-KEY) covers the padlock
    models BL0509/BL1209/BS1609. TouchLock variants that carry a fingerprint
    sensor but no BLE exist and are NOT covered here.

    The Android package name com.champion.lock points at the ODM ("Champion"),
    and the app recognizes advertised names that BIO-key never shipped
    (Joi_Lock), so this spec covers a shared OEM protocol across a family of
    rebadged locks, not only BIO-key-branded ones. Non-BIO-key siblings are
    assumed to speak the identical protocol because the same app drives them
    — that assumption is unverified (no capture of an OEM sibling exists).

    The most useful fact about this family: it has NO cloud at all. The app
    requests INTERNET permission but a full-decompile sweep of com.champion.**
    finds no HTTP client, endpoint, account system, sync or OTA mechanism
    (the settings screen's upgrade row is unconditionally hidden and the
    upgrade event class is empty). The lock cannot die from a cloud shutdown.

    Security assessment: there is no real cryptography. The rolling
    credential is the current timestamp yy-MM-dd-HH-mm-ss-SSSS split into
    eight two-digit decimal groups, each written as one byte parsed as hex.
    Protection rests on BLE bonding plus this obfuscation; anyone who
    observes one exchange and knows the approximate time can replay or
    predict credentials. For a local integration this is a convenience — a
    client can generate valid commands with no secrets beyond enrollment —
    but nobody should treat the lock's electronic path as high-security.

  identification:
    local_name_prefixes:
      - "TSA_BT"
      - "TSA_PLUS"
      - "XL_BT"
      - "XL_PLUS"
      - "Ble_Lock"
      - "Bike_BT"
      - "Bike_Pr"
      - "Joi_Lock"
      - "U_Lock"
      - "ULockPr"
    service_uuids:
      - "67ac8800-a0eb-11e6-80f5-76304dec7eb7"
    manufacturer_data:
      description: >
        The lock broadcasts manufacturer-specific data whose first entry's
        payload is a single status byte (same bit layout as the notify
        status byte — see `advertising`). The Bluetooth SIG company ID in
        the header is NOT recoverable from the decompile (the app reads the
        first SparseArray key, whatever it is) — it needs one capture. Until
        then, do not filter on company ID.
    # Note: whether 67ac8800-... is advertised in the scan response or only
    # present after connection is unverified; the app scans UNFILTERED and
    # selects candidates by name pattern plus presence of the manufacturer-
    # data status byte.

  discovery:
    methods:
      - type: "ble_scan"
        ble:
          local_name:
            match: "regex"
            value: "TSA_BT|TSA_PLUS|XL_BT|XL_PLUS|Bike_BT|Bike_Pr|ULockPr|LOCK"
            # Mirrors the app's DeviceNameUtils: explicit patterns for the
            # TSA/XL/bike names, then a case-insensitive "LOCK" substring
            # fallback (which is what catches Ble_Lock, Joi_Lock, U_Lock).
    identity:
      stable_keys: ["address"]
      display: "local_name"
      notes: >
        The app keys locks by their MAC address. Whether the address is
        public or random-static is unverified (no capture); if a unit turns
        out to rotate its address, key on the manufacturer-data status byte
        stream instead and re-resolve the address by scanning.
    static_ip_required: false

  setup:
    required: true
    confidence: medium
    notes: >
      No network provisioning exists (there is no radio but BLE), but the
      lock must be ENROLLED before it will open: an unregistered lock
      accepts RegisterAdmin (0x80), which installs an 8-byte admin ID and
      the first time-based password. A factory-fresh (or factory-reset) lock
      advertises its unregistered state in the status byte (bit 4 = 0).
      Confidence is medium: the full exchange is read out of the decompiled
      app and re-checked, but has not been run against hardware here.
    methods:
      - type: ble_provisioning
        verified: false
        description: >
          Admin enrollment over GATT: bond, enable notifications, then send
          RegisterAdmin with a fresh time-derived admin ID and password.
          Entirely lock<->client; no server is involved at any point.
        ble:
          pairing_required: true
          pin_source: >
            No PIN — Android createBond() is invoked (via reflection) and
            the lock apparently accepts Just Works bonding; error code 0x07
            (UnBondDevice) implies the lock refuses commands from unbonded
            centrals. Whether bonding is strictly required before writes is
            unverified — the app handles every pairing variant. Note the app
            deliberately UNPAIRS on disconnect (Android 7+), so bonds are
            transient in the official flow.
        steps:
          - action: "Scan unfiltered; select candidates by advertised name pattern and the presence of a manufacturer-data status byte."
            actor: client
          - action: "Connect and bond (Just Works). The lock may reject unbonded centrals with error 0x07."
            actor: client
          - action: "Discover services and enable notifications on 67ac8801-a0eb-11e6-80f5-76304dec7eb7 (CCCD 0x2902)."
            actor: client
            expect: "The app waits ~1.2 s after service discovery before sending its first command; mirror that delay."
          - action: "Check the status byte: bit 4 = 0 means unregistered."
            actor: client
          - action: "Send RegisterAdmin (0x80): payload A = fresh 8-byte admin ID (the app derives it from the current time, colon-joined hex), payload B = current time-based password (see `protocol.crypto`)."
            actor: client
            expect: "Notify response: opcode echo 80, error code 00 (success). The lock now reports registered (status bit 4 = 1)."
          - action: "Cache the admin ID and current password locally — they are the only credentials, and there is no recovery path other than factory reset."
            actor: client
    factory_reset:
      applicable: true
      confidence: low
      effect: >
        FactoryInit (0x8f) returns the lock to the unregistered state,
        clearing the admin ID, all enrolled users and the rolling password;
        every client must re-enroll afterwards. No physical reset procedure
        is documented in the app — reset is a BLE command, gated by the
        factory password. Confidence low: read from the app, never executed;
        on a lock, a botched reset flow can leave the owner locked out.
      procedures:
        - name: "FactoryInit BLE command (0x8f)"
          steps:
            - action: "Connect, bond, enable notifications as in enrollment."
              actor: client
            - action: "Send FactoryInit (0x8f): payload A = the 8-byte admin ID, payload B = the 8-character factory password, one hex-digit character per byte."
              actor: client
              expect: "Notify response: opcode echo 8f, error code 00; the status byte's registered bit clears."
              notes: >
                The app's own dialog hints the default factory password is
                00000000 (eight zero characters -> bytes 0x00 x8). A changed
                factory password can only be replaced via
                ChangeFactoryPasswordRequest (0x8c) + ChangeFactoryInitPasswordData
                (0x8d) while the old one is known. If both the admin
                credentials and a changed factory password are lost, no
                software recovery path is known.
    rejoin:
      in_place_supported: true
      requires_factory_reset: false
      notes: >
        Additional users are enrolled in place: the new client obtains the
        admin's current password out-of-band (the official app sends it by
        SMS — there is no server), sends UserRegistrationRequest (0x90)
        carrying that password plus its bytewise complement, then
        UserRegistration (0x91) to install its own admin ID and time-based
        password. RemoveUser (0x81) revokes a user. A factory reset is only
        needed when all credentials are lost.
    credentials:
      wifi_passphrase_protection: "not_applicable"
      stored_on_device:
        - "Admin ID (8 bytes) and current rolling time-based password"
        - "Enrolled user records (user IDs + their passwords)"
        - "Factory password (8 characters, default hinted as 00000000)"
        - "Lock RTC (synced by the client before every unlock)"
      issued_to_client:
        - "None device-generated — the client INVENTS the admin ID and password at enrollment and must store them"
      notes: >
        No WiFi exists anywhere in the product. All credentials are
        client-generated and pushed to the lock; losing the client's stored
        credentials means re-enrolling via factory reset.

cloud:
  required: false
  vendor_service: "None — no cloud service exists for this product"
  failure_mode: >
    Nothing breaks, because there is nothing to break: the decompiled app
    contains no HTTP client, endpoint, account system, cloud sync or OTA
    mechanism at all (full-decompile grep across com.champion.**; the
    manifest's INTERNET permission is unused by app code). The only
    vendor-disappearance risk is the TouchLock BT app being delisted from
    Google Play — the APK is archived (sha256 in `sources`) and everything
    the app does is reproducible from this spec.
  notes: >
    Recorded explicitly because a spec with no cloud block could read as
    'cloud not yet investigated'. Here the ABSENCE is the verified finding.
    Sharing between users is done out-of-band (the admin password is sent as
    an SMS), not via any server.

local_access:
  status: "native"
  covers:
    - "unlock (TimeStamp 0x01 then OpenLock 0x00) over local BLE with no cloud"
    - "admin and user enrollment/revocation (0x80/0x90/0x91/0x81), factory reset (0x8f)"
    - "find-my-lock (0xcc) and lock RTC sync (0x01)"
    - "passive lock-state + battery tracking from the manufacturer-data status byte in advertisements, without connecting"
  not_covered:
    - "firmware update — no OTA mechanism exists in the app or (apparently) the product"
    - "remote access — the product has no network interface at all; any remote control means a local BLE bridge (e.g. an ESPHome bluetooth_proxy host) within radio range"
    - "lock-side confirmation of everything here — no capture exists (see remaining_unknowns)"

ble:
  connection:
    transport: "le"
    bonding_required: true
    bonding_notes: >
      The app bonds via createBond() reflection and, on Android 7+, removes
      the bond again on disconnect — bonds are deliberately transient. The
      0x07 UnBondDevice error code implies the lock enforces bonding, but
      that is inference from the app's error handling, not a captured fact.
    write_notes: >
      Commands are written to 67ac8802-... with WRITE_TYPE_NO_RESPONSE
      (setWriteType(1) confirmed in BluetoothLeService). Responses arrive as
      notifications on 67ac8801-....

services:
  - uuid: "67ac8800-a0eb-11e6-80f5-76304dec7eb7"
    name: "TouchLock lock service"
    description: >
      The only application GATT service. 20-byte command frames are written
      to the write characteristic; responses and unsolicited status arrive
      on the notify characteristic. BaseScanActivity also defines a
      UUID_FILTER_SERVICE constant for 00001801 (Generic Attribute), but the
      actual scan is unfiltered (empty ScanFilter, batch mode) — 0x1801 is
      not known to be on the lock and is not listed as a service here. The
      standard Battery Service (0x180F / 0x2A19) appears in the bundled
      Nordic BleManager library but is apparently unused by the app flow —
      battery comes from the status byte instead.
    characteristics:
      - uuid: "67ac8801-a0eb-11e6-80f5-76304dec7eb7"
        name: "Command response / status (lock -> client)"
        type: "status"
        description: >
          Notify. Responses are parsed as a hex string: chars 0-1 echoed
          opcode, chars 2-3 error code, chars 4-5 status byte. Error codes:
          00 success, 01 invalid command, 02 checksum error, 03 incorrect
          password, 04 invalid user ID, 05 already registered, 06 user
          record full, 07 unbonded device. The lock also emits unsolicited
          status notifications headed by opcode ff (UpdateStatus). Enable
          via CCCD 0x2902 immediately after service discovery.
        properties: ["notify"]
      - uuid: "67ac8802-a0eb-11e6-80f5-76304dec7eb7"
        name: "Command write (client -> lock)"
        type: "command"
        description: >
          Write-without-response. Every command is exactly 20 bytes:
          [0] opcode, [1] 0x00, [2..9] payload A, [10..17] payload B,
          [18] 0x00, [19] checksum = (0x5A - sum(bytes[0..18])) & 0xFF.
          See `protocol.commands` for the opcode table.
        properties: ["write_without_response"]

advertising:
  discovery_method: "ble_scan"
  notes: >
    While disconnected the lock broadcasts a single status byte as the
    payload of the first manufacturer-specific-data entry — the same byte
    layout as notify responses (bit 7 lock state, bit 4 registered flag,
    bits 2..0 battery 0-7), so a passive observer can track lock state and
    battery without ever connecting. The Bluetooth SIG company ID in the
    manufacturer-data header is UNKNOWN (the app reads the first SparseArray
    entry regardless of key) and the exact AD structure around the status
    byte is unverified — both need one capture. The app itself scans
    unfiltered with a 2 s batch report delay inside a 10 s window.
  manufacturer_data:
    company_id: "unknown — do not filter on it"
    status_byte:
      bit_7: "lock state (polarity UNCONFIRMED; for bike-name devices the app forces 'locked' in its UI)"
      bit_4: "registered flag (1 = an admin is enrolled; the app forgets a known lock that reports 0)"
      bits_2_0: "battery level 0-7 (mapping to percentage unverified)"

protocol:
  framing:
    byte_order: "big-endian (single-byte fields; no multi-byte integers in any command)"
    checksum: >
      byte[19] = (0x5A - sum(bytes[0..18])) & 0xFF — additive checksum with
      a 0x5A seed, confirmed in CommandUtils.sumData. The same computation
      is applied to every command frame regardless of opcode.
    notes: >
      Fixed 20-byte command frame: [0] opcode, [1] 0x00, [2..9] payload A
      (8 bytes), [10..17] payload B (8 bytes), [18] 0x00, [19] checksum.
      Multi-byte 'values' such as the admin ID travel as eight independent
      bytes (colon-joined hex strings in the app), so endianness never
      arises. A separate 14-byte writeBlePeripheralCommand(adminId[0..5],
      00 00, 6-byte arg) exists in CommandUtils but has NO callers — dead
      code or factory-use, recorded so nobody chases it.

  crypto:
    primitives: ["none — obfuscation only"]
    rolling_password: >
      The 'time-based password' is the current timestamp formatted
      yy-MM-dd-HH-mm-ss-SSSS, split into eight two-digit decimal groups,
      each group written as one byte parsed as hexadecimal (Utils.
      getPasswordByTime). Example: 26-08-18-19-05-23-84-62 becomes bytes
      0x26 0x08 0x18 0x19 0x05 0x23 0x84 0x62. On a successful OpenLock the
      new time-based password carried in payload B becomes the stored
      password; the previous one is kept as a one-shot fallback (retried
      once on error 03, then the app aborts). The scheme is trivially
      predictable by anyone who has observed one exchange and knows the
      approximate time — treat BLE bonding as the only real (and weak)
      barrier, and note this spec exists precisely so owners are not locked
      out by it.
    enrollment_ids: >
      The admin ID is also time-derived: eight two-digit groups from the
      current time, joined with colons. A local client may use any 8-byte
      value; only uniqueness per lock matters (error 05 on re-registration).

  session_flow:
    description: >
      Unlock session as implemented by the app (BaseBluetoothActivity /
      MainActivity): scan -> connect -> bond -> discover services -> enable
      notifications on 67ac8801 (then a ~1.2 s pause). If the status byte
      shows unregistered, send RegisterAdmin (0x80). To open: send TimeStamp
      (0x01) to sync the lock RTC — success here is required before the app
      will proceed — then OpenLock (0x00) carrying the stored password in
      payload A and the new time-based password in payload B. On success the
      new password replaces the stored one. An optional app-level PIN
      (default 33333333) or phone fingerprint gates the unlock button —
      phone-side UI only, NOT part of the BLE protocol.
    verification: "reported"
    verification_note: "Reconstructed from the decompiled app's command dispatch; not replayed against a lock."

  responses:
    layout: "hex string: chars 0-1 opcode echo, 2-3 error code, 4-5 status byte"
    error_codes:
      "00": "success"
      "01": "invalid command"
      "02": "checksum error"
      "03": "incorrect password"
      "04": "invalid user ID"
      "05": "already registered"
      "06": "user record full"
      "07": "unbonded device"
    verification: "reported"
    verification_note: "ErrorCode enum confirmed in the decompile; per-opcode response nuances (GetMac 0xad is special-cased in the notify handler) are not fully mapped."

  commands:
    open_lock:
      id: 0
      hex: "0x00"
      name: "OpenLock"
      direction: "client_to_device"
      description: >
        Unlock. Payload A: the stored 8-byte unlock password. Payload B: the
        new time-based password, which becomes the stored password on
        success. The app always sends TimeStamp (0x01) first and retries
        once with the previous password on error 03.
      verification: "reported"
    time_stamp:
      id: 1
      hex: "0x01"
      name: "TimeStamp"
      direction: "client_to_device"
      description: >
        Sync the lock RTC: bytes 2-7 carry yy MM dd HH mm ss, each decimal
        field written as one hex-parsed byte; bytes 8-17 are zero. Must
        succeed before OpenLock in the app flow; whether the lock tolerates
        large clock skew is unknown.
      verification: "reported"
    register_admin:
      id: 128
      hex: "0x80"
      name: "RegisterAdmin"
      direction: "client_to_device"
      description: >
        Enroll the first/primary admin on an unregistered lock. Payload A:
        8-byte admin ID (client-generated, time-derived in the app).
        Payload B: the new time-based password.
      verification: "reported"
    remove_user:
      id: 129
      hex: "0x81"
      name: "RemoveUser"
      direction: "client_to_device"
      description: "Revoke an enrolled user. Payload A: admin ID. Payload B: the 8-byte user ID to remove."
      verification: "reported"
    get_user_info:
      id: 130
      hex: "0x82"
      name: "GetUserInfo"
      direction: "client_to_device"
      description: "Defined in CommandUtils but never dispatched by the app UI — response layout unknown."
      verification: "hypothesis"
    change_factory_password_request:
      id: 140
      hex: "0x8c"
      name: "ChangeFactoryPasswordRequest"
      direction: "client_to_device"
      description: "First half of changing the factory password. Payload A: admin ID. Payload B: admin ID again."
      verification: "reported"
    change_factory_password_data:
      id: 141
      hex: "0x8d"
      name: "ChangeFactoryInitPasswordData"
      direction: "client_to_device"
      description: "Second half of changing the factory password. Payload A: old factory password. Payload B: new factory password."
      verification: "reported"
      advanced: true
      advanced_reason: "Changes the password that gates factory reset. If the new value is lost and admin credentials are also lost, no software recovery path is known. Record the new password before sending."
    factory_init:
      id: 143
      hex: "0x8f"
      name: "FactoryInit"
      direction: "client_to_device"
      description: >
        Factory reset over BLE. Payload A: admin ID. Payload B: the
        8-character factory password, one hex-digit character per byte (the
        app dialog hints the default is 00000000). Clears admin and all
        users; the lock returns to the unregistered state.
      verification: "reported"
      advanced: true
      advanced_reason: "Wipes every enrolled credential on the lock; all clients must re-enroll afterwards. Mistyping the factory password just returns error 03, but a successful reset on a lock you cannot physically reach to re-enroll leaves it unusable remotely."
    user_registration_request:
      id: 144
      hex: "0x90"
      name: "UserRegistrationRequest"
      direction: "client_to_device"
      description: "First half of enrolling an additional user. Payload A: the current 8-byte admin password. Payload B: bytewise complement (XOR 0xFF) of payload A."
      verification: "reported"
    user_registration:
      id: 145
      hex: "0x91"
      name: "UserRegistration"
      direction: "client_to_device"
      description: "Second half of user enrollment. Payload A: admin ID. Payload B: the new user's time-based password."
      verification: "reported"
    get_mac:
      id: 173
      hex: "0xad"
      name: "GetMac"
      direction: "client_to_device"
      description: "Defined in CommandUtils and special-cased in the notify handler, but never dispatched by the app UI — response layout unverified."
      verification: "hypothesis"
    find_lock:
      id: 204
      hex: "0xcc"
      name: "FindLock"
      direction: "client_to_device"
      description: >
        Locate the lock (presumably audible/visual on the device — modality
        unconfirmed). Payloads are a fixed pattern: A = 22 44 66 88 88 66 44
        22, B = 11 33 55 77 77 55 33 11.
      verification: "reported"
    update_status:
      id: 255
      hex: "0xff"
      name: "UpdateStatus"
      direction: "device_to_client"
      description: "Header of the lock's unsolicited status notification (not a command the client sends)."
      verification: "reported"

  remaining_unknowns:
    - "Bluetooth SIG company ID of the manufacturer-data advertisement and the exact AD layout around the status byte (needs one capture)"
    - "Status-byte polarity (bit 7: 1 = locked or unlocked?) and the battery 0-7 to percentage mapping"
    - "Whether bonding is strictly enforced before writes, and which pairing method the lock proposes (error 0x07 implies enforcement)"
    - "GetMac (0xad) and GetUserInfo (0x82) response layouts (never dispatched in the app UI)"
    - "Whether OEM siblings (Joi_Lock etc.) run identical firmware — likely (same app) but never captured"
    - "Lock RTC tolerance to clock skew (TimeStamp must succeed before unlock)"
    - "The caller-less 14-byte writeBlePeripheralCommand — dead code or factory-use"
    - "Whether 67ac8800 is advertised in scan responses or only visible after connection"

helpful_urls:
  - title: "TouchLock BT on Google Play (com.champion.lock, developer Keyssmart)"
    url: "https://play.google.com/store/apps/details?id=com.champion.lock"
  - title: "APKCombo version history for com.champion.lock (older versions 2.1.21/2.1.22)"
    url: "https://apkcombo.com/touchlock-b/com.champion.lock/"
  - title: "FCC ID 2AIKJ-BL — TouchLock BT padlocks (models BL0509/BL1209/BS1609)"
    url: "https://fcc.report/FCC-ID/2AIKJBL"
  - title: "FCC manual mirror (model list BL0509/BL1209/BS1609)"
    url: "https://fccid.io/m/a149d2e83326ce6fab053504a390ff868f705671805d3463519992b539bceadb.pdf"
  - title: "BIO-key International (company status; SEC filings)"
    url: "https://www.bio-key.com"

sources:
  - type: "apk_decompilation"
    apk: "com.champion.lock v2.2.1 (versionCode 114), updated 2023-11-09, developer Keyssmart"
    sha256: "ab6084e6b5c66c7d39ca670837427e8833c16e4cc01dfdba11737f2016990a77"
    size_bytes: 3795119
    obtained_via: "idongdong mirror (apk.idongdong.com JSON API -> CDN down.sssdown.com), 2026-08; apkeep/apk-pure exited 0 without producing a file, google-play needed credentials"
    decompiler: "jadx"
    key_files:
      - "com/champion/lock/base/BaseScanActivity.java"
      - "com/champion/lock/base/BluetoothLeService.java"
      - "com/champion/lock/base/BaseBluetoothActivity.java"
      - "com/champion/lock/MainActivity.java"
      - "com/champion/lock/utils/CommandUtils.java"
      - "com/champion/lock/utils/DeviceNameUtils.java"
      - "com/champion/lock/utils/Utils.java"
      - "com/champion/lock/enums/ErrorCode.java"
      - "com/champion/lock/ui/fragment/SettingFragment.java"
  - type: "fcc_filing"
    url: "https://fcc.report/FCC-ID/2AIKJBL"
    description: "FCC ID 2AIKJ-BL, grantee BIO-KEY: TouchLock BT padlocks BL0509 (XS), BL1209 (XL), BS1609 (TSA)"
  - type: "vendor_store_listing"
    url: "https://play.google.com/store/apps/details?id=com.champion.lock"
    description: "Play Store listing (1,000+ installs as of 2026-08) — app identity, version 2.2.1, developer Keyssmart; no sibling apps found"
