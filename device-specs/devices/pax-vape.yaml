# PAX vaporizer device spec
# Copyright 2026 Pigs Can Fly Labs LLC
# SPDX-License-Identifier: Apache-2.0
#
# Enriched by reverse-engineering the official Android app `com.pax.app`
# ("PAX Mobile"), pulled from APKPure via apkeep and statically decompiled with
# jadx (clean-room, artifacts gitignored under workspace/). Cross-referenced
# against the independent third-party web reimplementation `pax-romana`
# (github.com/evertonstz/pax-romana). Evidence source files include:
#   com/pax/peace/encryption/b.java   AESEncryption.kt
#   com/pax/peace/encryption/a.java   AESCTREncryption.kt
#   com/pax/peace/encryption/i.java   EncryptionProvider.kt
#   com/pax/peace/g/q/l.java          decrypt path
#   com/pax/peace/models/u.java       PAXAttribute.kt
#   com/pax/peace/g/p/g.java          PaxServiceType.kt
#   com/pax/peace/g/p/b.java          AdvertisingInformation.kt
#   com/pax/peace/g/p/e.java          ManufacturerBitField.kt
#   com/pax/peace/g/p/c.java          DIS serial-number read
#   com/pax/peace/j/b.java            NumberExtensions.kt

device:
  name: "PAX Vaporizer"
  manufacturer: "PAX Labs"
  manufacturer_status: "unsupported"
  testing:
    status: untested
  protocol: "ble"
  category: "appliance"
  notes: >
    Supports PAX 3, PAX Era, PAX Era Pro. AES-128-OFB encryption applies to the
    Era and PAX 3; the Era Pro uses a more sophisticated protocol with nonces,
    AES-CTR mode and a connection handshake.
    Master key (hex): f7c866c38f78753086293bd57dd32540.
    This key is LOCALLY verified: it appears as the decimal byte array
    {247,200,102,195,143,120,117,48,134,41,59,213,125,211,37,64} at
    com/pax/peace/encryption/b.java:60 (which is why a text grep for the hex
    string "F7C866" finds nothing). It is also independently documented in public
    PAX reverse-engineering (e.g. blraaz.me / open-source pax libraries).
    Key derivation: read 8-char serial, duplicate to 16 bytes,
    AES-128-ECB encrypt with master key -> per-device key.
    Encrypt: AES-128-OFB with per-device key and random 16-byte IV.
    Wire format: [ciphertext] + [IV (16 bytes)]. On read, the LAST 16 bytes = IV.
    The IV deadbeefdeadbeefdeadbeefdeadbeef is the fixed IV for the AES-CTR
    pairing/enrollment path, not OFB (reclassified — see annotations below).
    Confirmed: com/pax/peace/encryption/i.java:23-27 builds the 16-byte array
    from bytes 222,173,190,239 (0xDE,0xAD,0xBE,0xEF) repeated 4x and returns it
    as the provider-wide IV (i.java:122-124); DeviceManager.java:692 passes it
    as the initVector to AESCTREncryption, whose constructor names the params
    "key"/"initVector" (encryption/a.java:14-19).
    Also has AES-128-CTR mode for initial pairing/enrollment: enrollment service
    8e320210 (g/p/g.java:133), internal write characteristic 8e320212
    (com/pax/peace/d.java:9, labelled INTERNAL_WRITE_CHARACTERISTIC at
    g/q/k.java:30-32). App-confirmed, not hardware-tested.
    Temperature encoding: uint16 LE, value / 10 = degrees C.
    Nordic Semiconductor chipset. FCC ID: 2AJWD-PAX3.
    PAX is explicitly allowed by project policy.

    Enrich-branch evidence annotations: the advertised 128-bit service UUID
    8e320200-64d2-11e6-bdf4-0800200c9a66 is from PaxServiceType.kt. BLE local
    name is user-customizable and therefore a weak signal. AdvertisingInformation.kt
    shows manufacturer-specific data contains a 2-byte status bitfield and 8-byte
    ASCII serial number after decryption; ManufacturerBitField.kt unpacks the
    little-endian bitfield, but exact semantics remain partly unresolved. Device
    Information Service serial-number characteristic 00002a25 is read to obtain
    the 8-character serial for key derivation. AESEncryption.kt confirms the
    master key byte array (b.java:60), the AES/ECB/NoPadding key-derivation
    cipher (b.java:53, encrypting the serial duplicated to 16 bytes at
    b.java:61-67) and the AES/OFB128/NoPadding data-plane cipher (b.java:58).
    The [ciphertext || IV] wire format is confirmed on the decrypt side:
    g/q/l.java:37-41 takes the IV from the LAST 16 bytes of the read value and
    decrypts the remainder. The enrich branch reclassified
    deadbeefdeadbeefdeadbeefdeadbeef as the fixed AES-CTR pairing/enrollment IV,
    not an OFB fallback (i.java:23-27). Headline UUIDs are all app-confirmed in
    PaxServiceType.kt / companion tables: main service 8e320200
    (g/p/g.java:182), read char 8e320201 and notify char 8e320203
    (g/p/f.java:31-34), write char 8e320202 (com/pax/peace/d.java:8).
    Attribute IDs are multiplexed by byte 0 and map to
    PAXAttribute.kt; STATUS_UPDATE and many per-attribute payloads remain
    documented in prose because the schema cannot express all variants.

    Ecosystem update (2026-07): PAX shut down its hosted Web Bluetooth app
    web-app.pax.com/device in May 2026 (it now redirects to pax.com); the
    Android app is at 6.4.1 and the iOS app has been off the App Store since
    Nov 2019. varenc/pax-app-bluetooth-server preserves the captured official
    web-app bundle and is the best public source for the Era Pro enrollment
    UUIDs. The TristWiki protocol notes (live site down; Wayback captures)
    archive a 33-entry attribute table including ENCRYPTION_EXCHANGE=49 and
    ENCRYPTION_PACKET=50 (Era Pro session-key negotiation). The app confirms
    attribute IDs 49 and 50 under the names KEY_EXCHANGE and ENCRYPTED_PACKET
    (models/u.java:51-52) — app-confirmed, not hardware-tested.
  identification:
    local_name_prefix: "PAX"
    service_uuids:
      - "8e320200-64d2-11e6-bdf4-0800200c9a66"

  discovery:
    methods:
      - type: "ble_scan"
        ble:
          local_name:
            match: "prefix"
            value: "PAX"
          service_uuids:
            - "8e320200-64d2-11e6-bdf4-0800200c9a66"
    identity:
      stable_keys: ["address"]
      display: "local_name"
    static_ip_required: false

  setup:
    required: false
    confidence: "medium"
    notes: >
      No network provisioning. The device does use an encrypted command channel,
      so a client must complete the documented key handshake after connecting —
      that is session setup, not onboarding.
    methods:
      - type: "ble_direct"
        verified: false
        description: >
          No provisioning step: the device advertises as soon as it is powered
          on and accepts a connection from any central. There is no account, no
          network credential exchange and no pairing PIN, so a replacement
          client only has to scan, connect and write.
        ble:
          advertised_name: "PAX"
          pairing_required: false
        steps:
          - action: "Power the device and scan for its advertisement."
            actor: "client"
            request:
              protocol: "ble_gatt"
          - action: "Connect and run the documented initialization sequence, if the spec defines one."
            actor: "client"
    factory_reset:
      confidence: "low"
      effect: >
        No credential state to clear. Power-cycling drops the current
        connection, which is the actual remedy for the common failure mode:
        the device already being connected to another central.
      procedures:
        - name: "Power cycle"
          steps:
            - action: "Remove power (or battery) for a few seconds and reapply."
              actor: "user"
              expect: "Device advertises again and accepts a new connection."
    rejoin:
      in_place_supported: true
      requires_factory_reset: false
      notes: >
        Nothing binds this device to a network or an owner, so switching
        controllers is just connecting from the new one. If the old client is
        an Android or iOS phone, remove the device from the OS Bluetooth list
        as well — a cached bond can keep the phone reconnecting automatically
        and holding the single available link.
    credentials:
      wifi_passphrase_protection: "not_applicable"
      stored_on_device: []
      issued_to_client: []
      notes: >
        Anything within radio range can connect and issue commands. Physical
        proximity is the only access control.

helpful_urls:
  - title: "Reverse Engineering Bluetooth Vapes -- Part 1"
    url: "https://blraaz.me/reverse-engineering/2021/08/29/bluetooth-reverse-engineering.html"
    description: "Public PAX Bluetooth reverse-engineering write-up cited by the device page and notes."
  - title: "tristanseifert/pax-controller-test"
    url: "https://github.com/tristanseifert/pax-controller-test"
    description: "Open-source PAX BLE controller (Swift, ISC); source of the protocol details cited by the device page. Archived upstream."
  - title: "varenc/pax-app-bluetooth-server"
    url: "https://github.com/varenc/pax-app-bluetooth-server"
    description: "Preserves the official PAX Web Bluetooth app bundle (captured April 2026 before the May 2026 shutdown); best public source for the Era Pro enrollment path."

# Custom consumer-side handler: derives the per-device OFB session key and
# frames [ciphertext || IV]. Declarative — names a future Rust handler.
protocol_handler: "pax_ofb"

services:
  - uuid: "8e320200-64d2-11e6-bdf4-0800200c9a66"
    name: "PAX Service"
    characteristics:
      - uuid: "8e320201-64d2-11e6-bdf4-0800200c9a66"
        name: "Read Data"
        properties: ["read"]
        encryption:
          algorithm: "aes-128-ofb"
          key_derivation: "device-specific"
          static_key: "F7C866C38F78753086293BD57DD32540"
        notes: >
          Read payload is AES-128-OFB encrypted; the LAST 16 bytes of each read = IV (ciphertext first).
          Confidence HIGH. Evidence: com/pax/peace/encryption/b.java:58 (AES/OFB128),
          b.java:60 (16-byte ECB key-derivation key), g/p/f.java:33 (this char UUID).
        format:
          # Layout of the DECRYPTED read packet: byte 0 is the attribute
          # (message type) ID, the attribute payload follows at byte 1
          # (tristanseifert PaxMessage.swift). Which payload field is valid
          # depends on byte 0 — the device stages one attribute per
          # request_status_update bitmask bit.
          - offset: 0
            length: 1
            name: "message_type"
            type: "uint8"
            notes: "Attribute ID: 0x01 = ACTUAL_TEMP, 0x02 = HEATER_SET_POINT, 0x03 = BATTERY, 0x18 = SUPPORTED_ATTRIBUTES (PaxMessageType enum)"
          - offset: 1
            length: 2
            name: "temperature"
            type: "uint16"
            endianness: "little"
            scale: 0.1
            unit: "C"
            notes: "Valid when message_type == 0x01 (ACTUAL_TEMP): uint16 LE, value / 10 = degrees C (GenericTemperatureMessage, tristanseifert PaxMessage.swift)"
          - offset: 1
            length: 1
            name: "battery"
            type: "uint8"
            unit: "%"
            notes: "Valid when message_type == 0x03 (BATTERY): state of charge 0-100 in one byte (BatteryMessage, tristanseifert PaxMessage.swift)"

      - uuid: "8e320202-64d2-11e6-bdf4-0800200c9a66"
        name: "Write Command"
        properties: ["write"]
        encryption:
          algorithm: "aes-128-ofb"
          key_derivation: "device-specific"
          static_key: "F7C866C38F78753086293BD57DD32540"
        notes: >
          static_key is the hardcoded AES-128-ECB KEY-DERIVATION key, NOT the
          payload key. The actual OFB session key =
          AES-ECB(static_key, peripheral_serial duplicated to 16 bytes); the write
          payload is then AES-128-OFB(session_key, random 16-byte IV) with the IV
          appended on the wire (ciphertext || IV). A secondary AES-128-CTR path
          (enrollment/pairing, key stored in the app DB) also exists. Confidence HIGH.
          Evidence: com/pax/peace/encryption/b.java:53,58,60,65,70,74;
          a.java:26 (AES/CTR); g/p/g.java:182 (service UUID);
          com/pax/peace/d.java:8 (this write char 8e320202; d.java:9 is the
          enrollment service's internal write char 8e320212, not a service UUID).
        commands:
          get_actual_temp:
            description: "Current temperature attribute (type 0x01). No public client polls with a bare attribute byte — the documented mechanism is request_status_update with the attribute's bit set, then a read; TristWiki marks ACTUAL_TEMP write unsupported."
            value: [0x01]
          set_heater_setpoint:
            description: "Set target temperature (encrypted, type 0x02, uint16 LE temp*10)"
            template: [0x02, "{temp_low}", "{temp_high}"]
            parameters:
              temp_low:
                type: "uint8"
                min: 0
                max: 255
              temp_high:
                type: "uint8"
                min: 0
                max: 255
          get_battery:
            description: "Battery attribute (type 0x03). As above: poll via request_status_update bitmask + read, not a bare [0x03] write."
            value: [0x03]
          set_lock:
            description: "Set lock status (encrypted, type 0x06, 0=unlock, 1=lock)"
            template: [0x06, "{lock_state}"]
            parameters:
              lock_state:
                type: "uint8"
                min: 0
                max: 1
          get_supported_attributes:
            description: "Supported-attributes bitfield (type 0x18). As above: poll via request_status_update bitmask + read. Per-model attribute support varies, so clients should rely on this rather than a fixed model table."
            value: [0x18]
          request_status_update:
            description: "Request status update: type 0xFE followed by an 8-byte LE bitmask of attribute IDs (a bare [0xFE] with no mask is malformed). The device stages the requested attributes for reading from the read characteristic."
            template: [0xFE, "{mask_0}", "{mask_1}", "{mask_2}", "{mask_3}", "{mask_4}", "{mask_5}", "{mask_6}", "{mask_7}"]
            parameters:
              mask_0:
                type: "uint8"
                min: 0
                max: 255
              mask_1:
                type: "uint8"
                min: 0
                max: 255
              mask_2:
                type: "uint8"
                min: 0
                max: 255
              mask_3:
                type: "uint8"
                min: 0
                max: 255
              mask_4:
                type: "uint8"
                min: 0
                max: 255
              mask_5:
                type: "uint8"
                min: 0
                max: 255
              mask_6:
                type: "uint8"
                min: 0
                max: 255
              mask_7:
                type: "uint8"
                min: 0
                max: 255

      - uuid: "8e320203-64d2-11e6-bdf4-0800200c9a66"
        name: "Notification Trigger"
        properties: ["read", "notify"]
        notes: >
          Data-ready notify characteristic. The notification carries a single
          byte — the first byte of the encrypted packet waiting on the read
          characteristic — and clients discard it and simply issue a read; it
          is NOT an OFB-encrypted payload.
          Confidence HIGH. Evidence: com/pax/peace/g/p/f.java:31 (this char UUID);
          single-byte semantics per blraaz.me and tristanseifert PaxDevice.swift.

  - uuid: "8e320210-64d2-11e6-bdf4-0800200c9a66"
    name: "PAX Enrollment Service"
    notes: >
      Era Pro pairing/enrollment service. Payloads use AES-128-CTR with the
      fixed enrollment IV deadbeefdeadbeefdeadbeefdeadbeef (see device notes);
      the session key is established during the enrollment handshake.
      App-confirmed, not hardware-tested.
      Evidence: g/p/g.java:133 (service UUID in PaxServiceType.kt).
    characteristics:
      - uuid: "8e320212-64d2-11e6-bdf4-0800200c9a66"
        name: "Internal Write"
        properties: ["write"]
        encryption:
          algorithm: "aes-128-ctr"
          key_derivation: "handshake"
        notes: >
          Enrollment write characteristic; the app frames writes as
          [attribute ID byte] + payload (g/q/k.java:33-38 prepends
          paxAttribute.valueToByte()). Attribute IDs include KEY_EXCHANGE=49
          and ENCRYPTED_PACKET=50 (models/u.java:51-52). CTR decrypt on this
          path uses the fixed deadbeef... initVector (DeviceManager.java:692,
          i.java:23-27,122-124; a.java:14-19 names the ctor params
          "key"/"initVector"). App-confirmed, not hardware-tested.
          Evidence: com/pax/peace/d.java:9 (UUID), g/q/k.java:30-32
          (INTERNAL_WRITE_CHARACTERISTIC).
      - uuid: "8e320213-64d2-11e6-bdf4-0800200c9a66"
        name: "Internal Read/Notify"
        properties: ["read", "notify"]
        notes: >
          Second enrollment characteristic: the app's notification routing
          table registers 8e320213 as BOTH the data-ready (notify) and the
          read characteristic of the enrollment service
          (g/p/f.java:44-49, INTERNAL_SERVICE_NOTIFICATION). App-confirmed,
          not hardware-tested.

entities:
  - platform: "sensor"
    name: "Temperature"
    device_class: "temperature"
    unit: "C"
    state_characteristic: "8e320201-64d2-11e6-bdf4-0800200c9a66"
    state_mapping:
      value: "temperature"
    notes: >
      Decrypted read packet with message_type 0x01 (ACTUAL_TEMP); stage it
      via request_status_update with bit (1 << 1) set, then read.

  - platform: "sensor"
    name: "Battery"
    device_class: "battery"
    unit: "%"
    state_characteristic: "8e320201-64d2-11e6-bdf4-0800200c9a66"
    state_mapping:
      value: "battery"
    notes: >
      Decrypted read packet with message_type 0x03 (BATTERY); stage it via
      request_status_update with bit (1 << 3) set, then read.
