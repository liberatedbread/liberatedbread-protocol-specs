{
  "camera": {
    "enable_notes": "Requires running docker-wyze-bridge with Wyze cloud credentials (API Key + ID). Stock Wyze cameras expose no local API on their own.\n",
    "enable_required": true,
    "feed_host_source": "device",
    "streams": [
      {
        "auth": "none",
        "default_port": 8554,
        "name": "Local RTSP (via bridge)",
        "notes": "Served by docker-wyze-bridge on the LAN; {bridge_address} is the host running the bridge.",
        "served_by": "device",
        "transport": "rtsp",
        "url_template": "rtsp://{bridge_address}:8554/{camera_name}"
      },
      {
        "auth": "none",
        "default_port": 8889,
        "name": "Local WebRTC (via bridge)",
        "notes": "Low-latency WebRTC endpoint from the bridge.",
        "served_by": "device",
        "transport": "webrtc",
        "url_template": "http://{bridge_address}:8889/{camera_name}"
      }
    ]
  },
  "device": {
    "category": "camera",
    "discovery": {
      "identity": {
        "display": "camera_name",
        "stable_keys": [
          "mac"
        ]
      },
      "methods": [
        {
          "mdns": {
            "identity_mapping": {
              "display": {
                "source": "cloud:wyze#nickname"
              },
              "stable_keys": [
                {
                  "key": "mac",
                  "source": "cloud:wyze#mac"
                }
              ]
            },
            "port": 8554,
            "service_type": "_http._tcp.local."
          },
          "notes": "Cameras are enumerated from the Wyze cloud account (not on-LAN); the bridge then re-serves each as rtsp://{bridge-ip}:8554/{camera-name}.\n",
          "type": "mdns"
        }
      ],
      "static_ip_required": false
    },
    "identification": {
      "default_port": 8554
    },
    "manufacturer": "Wyze Labs",
    "manufacturer_status": "active",
    "name": "Wyze Cam (via docker-wyze-bridge)",
    "notes": "Wyze cameras have NO first-party local API — control is cloud-app only. The community docker-wyze-bridge fills the gap: it authenticates to the Wyze cloud once (to enumerate cameras), connects to each camera over Wyze's TUTK peer-to-peer transport, and re-serves the video LOCALLY as RTSP/RTMP/HLS/ WebRTC. This is a \"cloud token to bootstrap, local stream thereafter\" case, not fully cloud-free.\nBRIDGE PORTS (what the bridge exposes on the LAN): RTSP 8554, HLS/HTTP 8888, WebRTC 8889, web UI 5000. Local stream URL: rtsp://{bridge-ip}:8554/{camera-name}.\nAUTH: Wyze cloud credentials required — email/password plus (since May 2024) an API Key + API ID from Wyze — to fetch camera info; media then streams locally via P2P. No special camera firmware is required; some newer firmware degrades remote P2P (local LAN usually still works).\n",
    "openness": {
      "notes": "Wyze publishes no local API; the bridge reverse-engineers Wyze's TUTK P2P transport and re-emits local streams. A one-time cloud login (API Key + ID) is required to enumerate cameras — the one target here that is not fully cloud-free.\n",
      "reverse_engineered": true,
      "source_code": "https://github.com/mrlt8/docker-wyze-bridge",
      "status": "hostile",
      "upstream_docs": [
        {
          "covers": "The exposed ports, the local RTSP URL and the cloud API-key requirement.",
          "title": "mrlt8/docker-wyze-bridge",
          "url": "https://github.com/mrlt8/docker-wyze-bridge"
        }
      ]
    },
    "pictogram": "ip-camera",
    "protocol": "wifi",
    "setup": {
      "confidence": "low",
      "credentials": {
        "issued_to_client": [
          "Wyze API Key + API ID (used by the bridge to enumerate cameras)"
        ],
        "notes": "Onboarding is app-mediated (not documented here). The bridge needs a Wyze API Key/ID to bootstrap; media then flows locally.\n",
        "stored_on_device": [
          "Wi-Fi credentials (on the camera)"
        ],
        "wifi_passphrase_protection": "unknown"
      },
      "factory_reset": {
        "applicable": "unknown",
        "confidence": "low",
        "effect": "Camera reset is done via the Wyze app / physical button (vendor cloud); the bridge holds no camera state to reset. Not transcribed here.\n"
      },
      "methods": [
        {
          "cloud": {
            "local_alternative": "Partial — media streams locally (the bridge prefers a LAN connection) once running, but enumerating the cameras requires a one-time Wyze cloud login (email/password + API Key/ID). There is no fully cloud-free path with stock firmware.\n"
          },
          "description": "Onboard the camera in the Wyze app, obtain a Wyze API Key + API ID, and run docker-wyze-bridge with them; it connects P2P and serves local RTSP/HLS/WebRTC.\n",
          "steps": [
            {
              "action": "Onboard the camera in the Wyze app.",
              "actor": "user"
            },
            {
              "action": "Get a Wyze API Key + API ID and run docker-wyze-bridge with them.",
              "actor": "user"
            },
            {
              "action": "Consume the local stream at rtsp://{bridge-ip}:8554/{camera-name}.",
              "actor": "client"
            }
          ],
          "type": "cloud_account",
          "verified": false
        }
      ],
      "notes": "The camera is onboarded through the Wyze app (cloud) as normal. Then run docker-wyze-bridge with the account's API Key + API ID + email to expose local streams. There is no account-free path — cloud login bootstraps the camera list.\n",
      "rejoin": {
        "in_place_supported": true,
        "notes": "Wi-Fi is reconfigured via the Wyze app; the bridge reconnects P2P afterward.",
        "requires_factory_reset": false
      },
      "required": true
    },
    "testing": {
      "notes": "Not driven against hardware by this project. The bridge ports, the local RTSP URL and the cloud-credential requirement are from the docker-wyze-bridge README.\n",
      "status": "untested"
    },
    "transport": "rtsp"
  },
  "helpful_urls": [
    {
      "description": "Community bridge: exposes Wyze cameras as local RTSP/RTMP/HLS/WebRTC; needs a Wyze API Key/ID.",
      "title": "mrlt8/docker-wyze-bridge",
      "url": "https://github.com/mrlt8/docker-wyze-bridge"
    }
  ]
}
