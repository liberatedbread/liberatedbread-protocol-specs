{
  "device": {
    "category": "hub",
    "discovery": {
      "identity": {
        "display": "name",
        "stable_keys": [
          "hub_id"
        ]
      },
      "methods": [
        {
          "mdns": {
            "identity_mapping": {
              "display": {
                "source": "name"
              },
              "stable_keys": [
                {
                  "key": "hub_id",
                  "source": "txt:id"
                }
              ]
            },
            "port": 8081,
            "service_type": "_smartthings._tcp.local.",
            "txt_record_keys": [
              "id",
              "type",
              "path"
            ]
          },
          "type": "mdns"
        },
        {
          "mdns": {
            "identity_mapping": {
              "display": {
                "source": "name"
              },
              "stable_keys": [
                {
                  "key": "hub_id",
                  "source": "txt:id"
                }
              ]
            },
            "port": 8766,
            "service_type": "_smartthings-hedge._tcp.local."
          },
          "type": "mdns"
        }
      ],
      "static_ip_required": false
    },
    "identification": {
      "default_port": 8081,
      "identity_keys": {
        "display": "name",
        "primary": "hub_id"
      },
      "mdns_service_type": "_smartthings._tcp.local."
    },
    "manufacturer": "Samsung SmartThings",
    "manufacturer_status": "active",
    "name": "SmartThings Hub v2",
    "notes": "SmartThings Hub v2 advertises local SmartThings, Edge driver, and Matter services via mDNS. The primary identity is the _smartthings._tcp TXT id value. _smartthings-hedge._tcp on port 8766 exposes Edge driver control features, and _matter._tcp advertises the hub's Matter operational node endpoint — the hub is a Matter CONTROLLER only, not a bridge: attached Zigbee/Z-Wave devices are not exposed to other Matter fabrics (no bridge-to-third-party-fabrics shipped through firmware 0.57.x). Security note: Cisco Talos disclosed 20 vulnerabilities (incl. RCE chains) in SmartThings Hub firmware 0.20.17 in 2018 (TALOS-2018-0539 … TALOS-2018-0581).\n",
    "protocol": "wifi",
    "setup": {
      "confidence": "low",
      "credentials": {
        "issued_to_client": [],
        "stored_on_device": [
          "Account binding token",
          "ZigBee and Z-Wave network keys"
        ],
        "wifi_passphrase_protection": "not_applicable"
      },
      "factory_reset": {
        "confidence": "low",
        "effect": "Unclaims the hub and clears its ZigBee/Z-Wave networks; every paired radio device must be re-joined afterwards.",
        "procedures": [
          {
            "hold_seconds": 30,
            "indicator": "Status LED turns yellow and flashes.",
            "name": "Reset button held while powering on",
            "steps": [
              {
                "action": "Remove power and batteries.",
                "actor": "user"
              },
              {
                "action": "Hold the recessed reset button on the back while restoring power, and keep holding until the LED flashes yellow.",
                "actor": "user",
                "notes": "Vendor-documented; verify before use — this drops every paired ZigBee and Z-Wave device."
              }
            ]
          }
        ]
      },
      "methods": [
        {
          "description": "Ethernet to the router; the hub takes a DHCP lease and advertises _smartthings._tcp.local.",
          "steps": [
            {
              "action": "Connect Ethernet, insert the batteries if fitted, and apply power.",
              "actor": "user",
              "expect": "Status LED settles to solid green once the hub reaches the service.",
              "timeout_seconds": 300
            }
          ],
          "type": "wired",
          "verified": false
        },
        {
          "cloud": {
            "local_alternative": "None known. Edge drivers execute locally once installed, but the claim step and driver distribution both require the cloud.\n",
            "provider": "Samsung SmartThings"
          },
          "description": "The welcome code on the hub is redeemed in the vendor app, which binds the hub to a Samsung account and provisions its drivers.",
          "steps": [
            {
              "action": "Redeem the welcome code printed on the underside of the hub in the vendor app.",
              "actor": "user"
            }
          ],
          "type": "cloud_account",
          "verified": false
        }
      ],
      "notes": "The hub is Ethernet-only and its onboarding is entirely cloud-mediated: the welcome code printed on the underside is redeemed against Samsung's service, and the hub receives its identity and driver set from there. Local Edge drivers run on the hub afterwards, but there is no documented way to claim an unclaimed hub without the cloud, which is the key risk if the service is retired — and there is precedent: Samsung shut down the hub v1 cloud on 2021-06-30, rendering those hubs useless.\n",
      "rejoin": {
        "in_place_supported": true,
        "notes": "Moving the hub to a different router is a cable move; the cloud claim is unaffected.",
        "requires_factory_reset": false
      },
      "required": true
    },
    "testing": {
      "status": "untested"
    },
    "transport": "https"
  },
  "entities": [
    {
      "name": "Hub Status",
      "platform": "sensor",
      "state_topic": "smartthings://{hub_id}"
    },
    {
      "name": "Matter Controller",
      "platform": "binary_sensor",
      "state_topic": "matter://{hub_id}"
    }
  ],
  "evidence": {
    "live_lan_probe": {
      "address": "10.69.199.162",
      "date": "2026-07-16",
      "hostname": "hubv2-0d052a8a662bc0001.local",
      "mdns_services": [
        {
          "id": "D052A8A662BC0001",
          "port": 8081,
          "service_type": "_smartthings._tcp",
          "type": "hubv2"
        },
        {
          "feat": "ctrl",
          "port": 8766,
          "service_type": "_smartthings-hedge._tcp"
        },
        {
          "T": 6,
          "port": 49722,
          "service_type": "_matter._tcp"
        }
      ]
    }
  },
  "http_endpoints": [
    {
      "description": "Primary _smartthings._tcp service endpoint on port 8081 (probe-observed only; the 2016 community capture's TXT path=/ supports a root path existing, but no public documentation of an HTTP API on 8081 exists). Edge control is advertised separately on the hedge HTTPS service.",
      "method": "GET",
      "name": "SmartThings Local Service Root",
      "path": "/",
      "status": "unverified"
    },
    {
      "description": "App-derived, not exercised on hardware: hedge HTTPS endpoint used by the vendor app's hub diagnostics flow. Authenticated with a cloud-issued HubEdgeAuthentication bearer token (see smartthings_local_protocol.edge_driver_service.auth).",
      "method": "GET",
      "name": "Hub Connection State (hedge, port 8766)",
      "path": "/devices/hub/connectionState",
      "status": "unverified"
    },
    {
      "description": "App-derived, not exercised on hardware: hedge HTTPS endpoint returning the hub's diagnostic logs, same bearer auth as /devices/hub/connectionState.",
      "method": "GET",
      "name": "Diagnostic Logs Download (hedge, port 8766)",
      "path": "/util/diagnosticLogsDownload",
      "status": "unverified"
    },
    {
      "description": "App-derived, not exercised on hardware: hedge HTTPS endpoint that reboots the hub, same bearer auth as /devices/hub/connectionState.",
      "method": "POST",
      "name": "Hub Reboot (hedge, port 8766)",
      "path": "/util/reboot",
      "status": "unverified"
    },
    {
      "description": "Undocumented local HTTPS API on port 9495 used by the official SmartThings CLI (edge:drivers:logcat). Requires Samsung OAuth bearer headers; TLS server certificate pinned to the hub identity.",
      "method": "GET",
      "name": "Edge Drivers (port 9495)",
      "path": "/drivers"
    },
    {
      "description": "Server-Sent Events live log stream on port 9495, same OAuth + TLS pinning as /drivers (official CLI logcat.ts).",
      "method": "GET",
      "name": "Edge Driver Logs SSE (port 9495)",
      "path": "/drivers/logs"
    }
  ],
  "smartthings_local_protocol": {
    "cloud_hedge_api": {
      "confidence": "medium",
      "notes": "App-derived, not hardware-verified: the vendor app also speaks a cloud-side \"hedge\" API family for Matter and Thread management (MatterService) — e.g. /hedge/request/{hubId}/devices/{deviceId}/matter/fabrics, /hedge/request/{hubId}/matter/rendezvous, and /hedge/request/{hubId}/thread/networkInfo — plus Matter DCL vendor/product lookups. These are cloud REST endpoints reached through Samsung's backend, not endpoints served by the hub itself.\n"
    },
    "edge_driver_service": {
      "auth": {
        "confidence": "medium",
        "notes": "App-derived: requests carry an Authorization: Bearer header built from a cloud-issued HubEdgeAuthentication (access_token plus a client public certificate), obtained from the cloud API via POST /hubdevices/{hubDeviceId}/hedge/auth.\n",
        "type": "bearer"
      },
      "confidence": "medium",
      "confidence_notes": "App-derived, not exercised on hardware: the vendor app's hub diagnostics flow discovers _smartthings-hedge._tcp.local. via mDNS and matches the endpoint against its L3_HTTPS protocol, then talks to it through a plain Retrofit HTTPS interface (iv/d). The feat=ctrl TXT key remains probe-only — the app never reads the hedge TXT record.\n",
      "notes": "Service and port are corroborated by community observation (Nov 2024) and by the vendor app's mDNS discovery. The transport is HTTPS, not WebSocket: the app matches the discovered endpoint against DiagnosticsProtocol.L3_HTTPS. The feat=ctrl TXT key rests solely on this spec's own probe — treat it as unverified.\n",
      "port": 8766,
      "service_type": "_smartthings-hedge._tcp.local.",
      "transport": "HTTPS",
      "txt": {
        "feat": "ctrl"
      }
    },
    "local_https_api": {
      "notes": "Undocumented local API used by the official SmartThings CLI (edge:drivers:logcat, defaultLiveLogPort = 9495): GET /drivers and GET /drivers/logs (Server-Sent Events), authenticated with Samsung OAuth bearer headers; the CLI pins the hub's TLS server certificate against its identity. Source: SmartThingsCommunity/smartthings-cli src/commands/edge/drivers/logcat.ts.\n",
      "port": 9495,
      "transport": "HTTPS"
    },
    "matter_controller": {
      "confidence": "unverified",
      "notes": "Hub v2 is a CSA-certified Matter CONTROLLER (not a bridge — see device notes). TXT T=6 is the Matter operational-discovery kTcpSupported bitmask (TCP client + server). Port 49722 is probe-observed only: Matter operational discovery uses ephemeral ports, so treat it as instance-specific, not normative.\n",
      "port": 49722,
      "service_type": "_matter._tcp.local.",
      "txt": {
        "T": 6
      }
    },
    "primary_service": {
      "port": 8081,
      "service_type": "_smartthings._tcp.local."
    }
  }
}
